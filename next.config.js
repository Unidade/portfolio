/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  // Necessary to build on GitHub Pages
  basePath: '/Unidade.github.io',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
